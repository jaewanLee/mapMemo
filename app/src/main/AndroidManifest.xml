<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jaewanlee.mapmemo">

    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".Core.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activity.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.LockScreen.LockScrennActivity"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait" />
        <activity android:name=".Login.Login" />

        <receiver android:name=".Core.BoradcastReceiver.RestartReceiver" />
        <receiver
            android:name=".Core.BoradcastReceiver.ScreenReceiver"
            android:process=".remote">
            <intent-filter>
                <action android:name="RestartReceiver.restart" />
            </intent-filter>
        </receiver>

        <service android:name=".Core.Service.ScreenService" />
        <activity android:name=".Activity.LocationBaseActivity"
            android:launchMode="singleTask">
        </activity>
        <activity android:name=".Activity.TimeBaseActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".Activity.SettingActivity"
            android:launchMode="singleInstance"/>
        <activity android:name=".Activity.FAQActivity"
            android:launchMode="standard"/>
        <activity android:name=".Activity.AccessTermActivity"
            android:launchMode="standard"></activity>
    </application>

</manifest>